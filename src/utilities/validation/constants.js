// This module exports two constants min and expires. 
// The min constant is a number with a value of 5, and 
// the expires constant is a string with a value of '12h'.

// These constants could be used for setting minimum 
// password length and expiration time for tokens or 
// cookies, for example.
const min = 5;
const expires = '12h';

module.exports = {min, expires};
